get:
  tags:
    - Common
  parameters:
    - name: tableName
      in: path
      required: true
      schema:
        type: string
  responses:
    default:
      description: 'Error'
      content:
        application/json:
          schema:
            $ref: '../_components/error.yaml'
